# CasoPractico2_AI-Act-PPTX-Generator

# 🇪🇺 AI-Act-PPTX-Generator

Este repositorio contiene un script de Python que genera automáticamente una presentación de PowerPoint (`.pptx`) con el resumen del **Reglamento Europeo de IA (AI Act)**, utilizando la librería `python-pptx` y un diseño institucional.

## 🚀 Uso (Ejecución)

Para generar la presentación necesitas tener Python instalado y la librería `python-pptx`.

### 1. Entorno Colab o Local

Si usas **Google Colab** (Recomendado):
```bash
!pip install python-pptx
# Luego, pega el contenido del script y ejecútalo.

Si lo usas Localmente (en tu PC):
# Instala la librería
pip install python-pptx
# Ejecuta el script
python generate_ai_act_ppt.py

### 2. Resultado

El script generará un archivo llamado CasoPractico2_AliciaB_Reglamento_IA_Act.pptx en la misma carpeta.
